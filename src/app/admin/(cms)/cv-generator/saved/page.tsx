import SavedCvManager from "@/components/CVGenerator/SavedCvManager";

export const dynamic = "force-dynamic";
export const metadata = { title: "CV enregistrés — Admin" };

export default function SavedCvPage() {
  return <SavedCvManager />;
}
